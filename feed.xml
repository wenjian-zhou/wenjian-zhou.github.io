<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://wenjian-zhou.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://wenjian-zhou.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-03-21T02:56:23+00:00</updated><id>https://wenjian-zhou.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Derive RIS from scratch</title><link href="https://wenjian-zhou.github.io/blog/2026/derive-ris/" rel="alternate" type="text/html" title="Derive RIS from scratch"/><published>2026-03-20T17:00:00+00:00</published><updated>2026-03-20T17:00:00+00:00</updated><id>https://wenjian-zhou.github.io/blog/2026/derive-ris</id><content type="html" xml:base="https://wenjian-zhou.github.io/blog/2026/derive-ris/"><![CDATA[<p>This post starts from the simplest possible integral and works toward the core idea behind RIS. The goal is to show, in a rough but direct way, how you might derive the RIS used in ReSTIR from scratch. By the time we reach the final form, the familiar RIS sampling procedure also falls out naturally.</p> <p>This is mainly for readers who want to understand why RIS converges, not just memorize the usual sequence of steps.</p> <p>If you want to justify RIS, the first step is to justify IS, or Importance Sampling. After that, you still need to show that resampling from those samples is also valid.</p> <p>In rendering, what we are fundamentally doing is solving the rendering equation. To keep the discussion easy to read, we reduce the rendering equation to the most basic integral:</p> \[F = \int f(x)\,dx\] <p>(If we want to emphasize the integration domain, we could write $F = \int_\Omega f(x)\,dx$, but we will omit $\Omega$ below.)</p> <p>Let us begin with importance sampling. A small but useful trick in the derivation is to multiply the integrand by 1. That “1” can be written using any probability density you like, as long as the integration domain matches. Let that density be $g(x)$. If we apply importance sampling and treat $\frac{f(x)}{g(x)}$ as a single quantity, with $g(x)$ being the density we introduced, we get:</p> \[F = \int f(x) \frac{g(x)}{g(x)} = \int \frac{f(x)}{g(x)}\,g(x)\,dx = \mathbb{E}_{X\sim g}\left[\frac{f(X)}{g(X)}\right]\] <p>This formula tells us that no matter which probability density $g(x)$ we sample from, the estimator still converges to the correct result. In that sense, the statement is almost trivial: it even includes the case where we do nothing especially clever and simply choose a density such as a uniform one. If we want to discuss which density is actually better, then the right quantity to study is variance rather than expectation, but that is outside the scope of this post.</p> <p>Once we have this trick in hand, we can push it a little further. Again we multiply by 1, but this time both the numerator and denominator are replaced by the full integral itself:</p> \[F = \frac{\int f(x)\,dx}{\int f(x)\,dx} \int f(x)\,dx\] <p>Next, multiply each integral inside by 1 once more. This time, the “1” is exactly the one we used above for importance sampling:</p> \[F = \frac{\int \frac{f(x)}{g(x)}\,g(x)\,dx}{\int \frac{f(x)}{g(x)}\,g(x)\,dx} \int \frac{f(x)}{g(x)}\,g(x)\,dx\] <p>Now rewrite the expression in expectation form:</p> \[F = \frac{\mathbb{E}_g[\frac{f(x)}{g(x)}]}{\mathbb{E}_g[\frac{f(x)}{g(x)}]} \int \frac{f(x)}{g(x)}\,g(x)\,dx\] <p>Now let:</p> \[w_i = \frac{f(x_i)}{g(x_i)}\] <p>If we use $N$ samples $x_1, \dots, x_N$ to approximate the expectation, we get:</p> \[F = \frac{\frac{1}{N}\sum^{N}_{i = 1} w_i}{\frac{1}{N}\sum^{N}_{j = 1} w_j}\int \frac{f(x)}{g(x)}\,g(x)\,dx\] <p>Now pull each term $w_i$ out of the numerator separately and drop the common factor $\frac{1}{N}$:</p> \[F = \sum^{N}_{i = 1} \frac{w_i}{\sum^{N}_{j = 1}w_j}\int \frac{f(x)}{g(x)}\,g(x)\,dx\] <p>At this point, $\frac{w_i}{\sum^{N}_{j = 1}w_j}$ can be interpreted as a probability $q_i$:</p> \[F = \sum^{N}_{i = 1} q_i\int \frac{f(x)}{g(x)}\,g(x)\,dx\] <p>Then rewrite the integral on the right-hand side as an expectation:</p> \[F = \sum^{N}_{i = 1} q_i \cdot \mathbb{E}_g[\frac{f(x)}{g(x)}]\] <p>Now comes the key observation. If we treat that expectation as a whole, then multiply it by another probability and sum over it, we get yet another expectation, this time taken with respect to $q$:</p> \[F = \mathbb{E}_q[\mathbb{E}_g[\frac{f(x)}{g(x)}]]\] <p>This is the end of the derivation. What we have shown is that if we first perform importance sampling, and then resample from the candidates drawn from $g$ using a resampling distribution $q$, the final expectation still evaluates to the solution of the rendering equation.</p> <p>The notation used in the SIGGRAPH 2023 course notes for <em>A Gentle Introduction to ReSTIR</em> looks slightly different, so it is useful to map the symbols one by one.</p> <p>In the PDF, $\omega_i = \frac{\hat{p}(X_i)}{p(X_i)}$, which corresponds to $\frac{f(x)}{g(x)}$ in this article. The Unbiased Contribution Weight is defined as $W_X = \frac{1}{\hat{p}(X)}(\frac{1}{M}\sum^{M}<em>{i=1}\omega_i)$. Since this is the reciprocal of the probability density we want to sample from, we can flip it over. If we also treat $\frac{1}{M}$ as a probability density $g$, then in the notation of this article it becomes $\frac{1}{W_X} = \frac{\hat{p}(X)}{g(X)}(\frac{1}{\sum^{M}</em>{i=1}\omega_i})$, which is exactly the form $\frac{\omega_j}{\sum^{M}_{i=1}\omega_i}$.</p> <p>At that point, we have derived the RIS used in ReSTIR by hand.</p>]]></content><author><name></name></author><category term="rendering"/><category term="rendering"/><category term="math"/><summary type="html"><![CDATA[derive RIS from scratch]]></summary></entry></feed>